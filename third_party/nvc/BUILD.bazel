load("@rules_foreign_cc//foreign_cc:defs.bzl", "configure_make")
load("@bazel_skylib//rules:build_test.bzl", "build_test")

package(default_visibility = ["//visibility:public"])

build_test(
    name = "nvc",
    tags = ["manual"],
    targets = [":nvc_repo"],
    visibility = ["//:__pkg__"],
)

configure_make(
  name = "nvc_repo",
  deps = [
    "@zlib//:zlib",
    "@libffi//:libffi",
    "@zstd//:zstd",
    "@elfutils//:elfutils",
  ],
  build_data = [
    "@elfutils//:elfutils",
    "@flex//:flex_exe",
    "@libffi//:libffi",
    "@zlib//:zlib",
    "@zstd//:zstd",
  ],
  lib_source = "@nvc//:all_srcs",
  visibility = ["//visibility:public"],
  out_binaries = [ "nvc", ],
  out_data_dirs = [ "lib", "libexec", "man", ],
  configure_options = [
    "--enable-llvm",
    #"--with-llvm=/usr/bin/llvm-config",
  ],
  install_prefix = "_install",
  env = {
    "LDFLAGS": "-lz -lgcc_s",
    "PATH": "$$(dirname $$EXT_BUILD_ROOT$$/$(location @flex//:flex_exe)):$$PATH",
    "libffi_LIBS": "-L$$(echo $$EXT_BUILD_DEPS$$)/lib -lffi",
    "libffi_CFLAGS": "-I$$(echo $$EXT_BUILD_DEPS$$)/include",
    "libzstd_LIBS": "-L$$(echo $$EXT_BUILD_DEPS$$)/lib -lzstd",
    "libzstd_CFLAGS": "-I$$(echo $$EXT_BUILD_DEPS$$)/include",
    "libdw_LIBS": "-L$$(echo $$EXT_BUILD_DEPS$$)/YYYelfutils/lib -ldw -lasm -lelf",
    "libdw_CFLAGS": "-I$$(echo $$EXT_BUILD_DEPS$$)/XXXelfutils/include",
  },
)

filegroup(
  name = "compiler",
  srcs = [":nvc_repo"],
  output_group = "nvc",
  visibility = ["//visibility:public"],
)

